ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.example.ApplicationKt.main ]
    }
}

# Database defaults (override via environment variables: DB_URL, DB_USER, DB_PASSWORD)
# PostgreSQL: jdbc:postgresql://127.0.0.1:5432/aimusic
# H2: jdbc:h2:mem:aimusic;DB_CLOSE_DELAY=-1
