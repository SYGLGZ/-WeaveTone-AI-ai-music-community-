<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug builds may connect to a backend on the local LAN over HTTP. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
